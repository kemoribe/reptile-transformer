# Analysis scripts

These are plotting helpers retained from the experiment workspace. Some scripts contain manually entered values or expect result files that are not distributed in this preview.

Do not treat generated figures as validated release results until the corrected ESM-2 experiments have been rerun, the inputs have been replaced with the final result files, and the values have been checked against `results/experiment_manifest_template.csv`.
